# run-publish-test-26.2

NeoForge **26.2** bare test game directory (Real Geology dev run only — no GeoStrata).

- Pre-seed `config/realgeology-common.toml` with `worldgen_mode = "section"` before first world (or copy from 1.21.1 publish test).
- Launch: `./scripts/launch-test-instance-26.2.sh` or `./gradlew :neoforge-26.2:runPublishTestClient`.
